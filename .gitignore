node_modules/
dist/
build/
.env
!.env.example
*.local

apps/api/generated/
apps/api/uploads/

.DS_Store
*.log
npm-debug.log*

# Backups locais (banco + uploads) — nunca versionar
backups/

# Env de produção
.env.prod

# Scratch de execução do superpowers (ledgers/briefs de subagent-driven-development)
.superpowers/

# Worktrees isolados de trabalho em andamento
.worktrees/

# Config local das ferramentas de desenvolvimento (não faz parte do produto)
.claude/

# Certificado TLS local (mkcert) — chave privada, nunca versionar
certs/
